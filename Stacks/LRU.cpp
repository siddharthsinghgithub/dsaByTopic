#include <bits/stdc++.h>
// 
using namespace std;
// 
// class LRU
// {
	// private:
		// const int CSIZE = 3;
		// unordered_map<int,list<int> :: iterator> M;
		// list<int> L;
	// public:
		// int find(int N){
			// cout<<"Searching in "
			// if( M.find(N) == M.end()){
				// cout<<"Not found in cache"<<endl;
				// if(L.size() == CSIZE)
				// {	
					// cout<<"Cache is full, deleting the last element : "<<L.back()<<endl;
					// M.erase(L.back());
					// L.pop_back();
				// }
			// }
			// else{
				// cout<<"Found in cache";
				// L.erase(M[N]);
			// }
			// 
			// L.push_front(N);
			// M[N] = L.begin();
		// }
// 
// };
// 

int main(){

	unsigned int x = 3, y = 5;

    cout<<(int)(x - y);

	return 0;
}